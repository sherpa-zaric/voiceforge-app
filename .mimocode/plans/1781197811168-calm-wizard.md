# UI/页面设计修复计划

## 修复范围
按优先级处理 UI 和页面设计问题。

---

## 任务 1: AI Tasks 页面表格优化

**问题**: 表格过宽，内容溢出，Result 列展示丑陋

**文件**: `src/app/[locale]/(landing)/activity/ai-tasks/page.tsx`

**修改内容**:
1. 移除 `provider` 列（已包含在 model 中）
2. 添加响应式列隐藏：
   - `mediaType`: `hidden md:table-cell`
   - `model`: `hidden lg:table-cell`
   - `costCredits`: `hidden sm:table-cell`
   - `createdAt`: `hidden sm:table-cell`
3. 限制 `prompt` 列宽度 `max-w-[200px] truncate`
4. 添加 `overflow-x-auto` 到表格容器

**文件**: `src/shared/blocks/table/table-card.tsx`
- CardContent 添加 `overflow-x-auto`

---

## 任务 2: Result 列重新设计

**问题**: AudioPlayer 太大（w-80），下载按钮缺失

**修改内容**:
1. AudioPlayer 宽度从 `w-80` 改为 `w-full`（在容器内自适应）
2. 为音频任务添加下载按钮（SVG 下载图标）
3. 图片展示改为 `h-16 w-16` 缩略图网格
4. 错误信息添加背景色突出显示

---

## 任务 3: 清理死代码

**文件和操作**:
1. `src/shared/blocks/dashboard/form-card.tsx` - 删除（占位符，零导入）
2. `src/shared/blocks/chat/suggestions.tsx` - 删除（硬编码，零导入）
3. `src/shared/blocks/email/verification-code.tsx` - 删除（零导入）
4. `src/shared/components/magicui/retro-grid.tsx` - 删除
5. `src/shared/components/magicui/text-shimmer.tsx` - 删除
6. `src/shared/components/magicui/meteors.tsx` - 删除
7. `src/shared/components/magicui/avatar-circles.tsx` - 删除
8. `src/shared/components/magicui/ripple.tsx` - 删除
9. `src/shared/components/magicui/particles.tsx` - 删除

---

## 任务 4: 修复注释掉的代码

**文件和操作**:
1. `src/shared/blocks/common/audio-player.tsx` - 移除注释掉的音量控制代码（第 139-160 行）
2. `src/shared/blocks/sign/sign-in-form.tsx` - 移除注释掉的忘记密码/记住我代码
3. `src/shared/blocks/chat/library.tsx` - 移除注释掉的下拉菜单代码

---

## 任务 5: 修复空壳页面

**文件**: `src/app/[locale]/(landing)/activity/feedbacks/page.tsx`

**选项**:
- A) 实现反馈列表页面（查询数据库中的反馈记录）
- B) 移除此页面和导航入口

**建议**: 如果没有反馈功能，选择 B 移除

---

## 任务 6: Empty 组件改进

**文件**: `src/shared/blocks/common/empty.tsx`

**修改内容**:
- 添加图标（如 Inbox 或 FileX）
- 添加可选的描述文本
- 添加可选的操作按钮

---

## 任务 7: LazyImage props 修复

**文件**: `src/shared/blocks/common/lazy-image.tsx`

**修改内容**:
- 将 `fill`, `priority`, `sizes` props 传递给底层组件

---

## 验证方式

1. 运行 `npx tsc --noEmit` 确认类型检查通过
2. 访问 `http://localhost:3000/activity/ai-tasks?type=audio` 验证表格布局
3. 访问 `http://localhost:3000/tts` 验证 TTS 功能正常
4. 检查删除的组件没有被其他地方引用

---

## 执行顺序

1. 任务 1 + 2（AI Tasks 页面优化）→ 验证
2. 任务 3（清理死代码）→ 验证
3. 任务 4（修复注释代码）→ 验证
4. 任务 5（空壳页面）→ 验证
5. 任务 6 + 7（组件改进）→ 验证
